# Crypto Exchange Arbitrage Platform

[🇬🇧 English version](README_EN.md)

## 📋 Описание проекта

Веб-приложение для анализа арбитражных возможностей на криптовалютных биржах и экспорта исторических данных криптопар. Поддерживает работу с 5 крупнейшими биржами: **Bybit**, **Bitget**, **KuCoin**, **OKX**, **HTX**.

### Основные возможности

- **📊 Анализ арбитража в реальном времени** - поиск лучших возможностей для арбитражной торговли
- **🔄 Матрица сравнения цен** - сравнение цен покупки/продажи между всеми биржами для выбранной пары
- **📈 Экспорт исторических данных** - выгрузка исторических свечей (OHLC) в Excel
- **✅ Проверка символов** - валидация торговых пар на биржах
- **⚡ WebSocket соединение** - отслеживание активности пользователей

---

## 🛠 Технологический стек

### Backend
- **Java 21**
- **Spring Boot 3.5.5**
    - Spring Web
    - Spring WebFlux
    - Spring WebSocket
    - Spring DevTools
- **Lombok** - упрощение кода
- **Jackson** - работа с JSON
- **Apache POI 5.4.1** - генерация Excel файлов
- **Java HTTP Client** - запросы к API бирж
- **Maven** - сборка проекта

### Frontend
- HTML5, CSS3, JavaScript (Vanilla JS)
- WebSocket для real-time обновлений

### API интеграции
- Bybit API v5
- Bitget API v2
- KuCoin API v1/v2
- OKX API v5
- HTX (Huobi) Market API

---

## 🚀 Быстрый старт

### Требования
- Java 21
- Maven 3.6+
- 2GB свободной оперативной памяти

### Установка и запуск

#### Вариант 1: Запуск из исходников

1. **Клонируйте репозиторий:**
```bash
  git clone <repository-url>
  cd Exchange
```

2. **Соберите проект:**
```bash
  mvn clean install
```

3. **Запустите приложение:**
```bash
  mvn spring-boot:run
```

4. **Откройте браузер:**
    - Приложение автоматически откроет браузер по адресу `http://localhost:8081`
    - Если автоматически не открылось - перейдите вручную

#### Вариант 2: Запуск через JAR файл

1. Скачайте последний релиз:
    - Перейдите в раздел [Releases](https://github.com/burbone/exchange-to-excel/releases/tag/v2.0.0)
    - Скачайте файл Exchange-2.0.0-SNAPSHOT.jar
2. Запустите приложение:

```bash
  java -jar Exchange-2.0.0-SNAPSHOT.jar
```
#### После запуска:
- Приложение автоматически откроет браузер по адресу http://localhost:8081
- Если автоматически не открылось - перейдите вручную
  #### Сборка JAR файла вручную:
```bash
    mvn clean package
    # JAR файл будет в target/Exchange-2.0.0-SNAPSHOT.jar
```
---

## 📖 Использование

### 1. Онлайн арбитраж (online.html)

**Топ-5 арбитражных возможностей:**
- Автоматическое обновление каждую секунду
- Показывает лучшие пары для арбитража
- Отображает биржи для покупки/продажи и процент прибыли

**Арбитражная матрица:**
- Введите символ торговой пары (например: `BTC USDT`)
- Матрица покажет разницу цен между всеми биржами

### 2. Экспорт исторических данных (data.html)

**Шаг 1:** Проверка символа
- Выберите биржу
- Введите символ (например: `BTC USDT`, `ETH BTC`)
- Нажмите "Проверить"

**Шаг 2:** Настройка параметров
- Укажите период (начало и конец)
- Выберите интервал свечей (1m, 5m, 15m, 1h, 4h и т.д.)

**Шаг 3:** Выбор данных
- Добавьте колонки (кнопка "+")
- Для каждой колонки выберите:
    - Тип данных: Open, High, Low, Close
    - Биржу: Bybit, Bitget, KuCoin, OKX

**Шаг 4:** Скачивание
- Нажмите "Скачать файл"
- Получите Excel файл с историческими данными

---

## 🏗 Архитектура проекта

### Структура пакетов

```
com.Exchange.Exchange
├── api/                    # API клиенты бирж
│   ├── bitget/
│   ├── bybit/
│   ├── htx/
│   ├── kucoin/
│   └── OKX/
├── config/                 # Конфигурация Spring
│   ├── ActivityChecker     # Проверка активности
│   ├── SchedulerConfig     # Настройка планировщика
│   └── WebSocketConfig     # Конфигурация WebSocket
├── controller/             # REST контроллеры
│   ├── data/              # Экспорт данных
│   └── tickers/           # Тикеры и арбитраж
├── dto/                   # Data Transfer Objects
│   ├── CustomCollections/ # Тикеры, матрицы
│   ├── Request/          # Запросы
│   └── Response/         # Ответы
├── handler/              # WebSocket обработчики
├── service/              # Бизнес-логика
│   ├── data/            # Валидация символов
│   ├── exel/            # Генерация Excel
│   │   └── Connecter/   # Коннекторы к биржам
│   └── tickers/         # Работа с ценами
└── ExchangeApplication   # Главный класс
```
---

## 🔧 API Endpoints

### Data Export

**POST** `/api/exportfile`
```json
{
  "symbol": "BTC USDT",
  "startTime": "2024-01-01T00:00",
  "endTime": "2024-01-02T00:00",
  "interval": "1h",
  "columns": [
    {"type": "open", "exchange": "bybit"},
    {"type": "close", "exchange": "bitget"}
  ]
}
```
Возвращает Excel файл с историческими данными.

### Symbol Validation

**POST** `/api/symbolcheck`
```json
{
  "exchange": "Bybit",
  "symbol": "BTC USDT"
}
```
Возвращает: `{"ok": true/false}`

### Arbitrage

**GET** `/api/top5`

Возвращает топ-5 арбитражных возможностей:
```json
[
  {
    "symbol": "BTCUSDT",
    "buyExchange": "Bybit",
    "sellExchange": "OKX",
    "buyPrice": 43500.0,
    "sellPrice": 43650.0,
    "profitPercent": 0.345
  }
]
```

**GET** `/api/matrix/{symbol}`

Возвращает арбитражную матрицу для указанного символа.

---

## ⚙️ Конфигурация

### Основные настройки (`application.properties`)

```properties
server.port=8081
spring.application.name=Exchange
logging.level.com.Exchange.Exchange=INFO
```

### Настройка параллелизма

В коннекторах (`Kline*Connect`):
```java
maxThreads = Math.max(2, Math.min(Runtime.getRuntime().availableProcessors() - 1, 8));
maxConcurrentRequests = 6;
```

### Интервалы обновления

- **PriceRepo**: обновление цен каждую 1 секунду
- **ActivityChecker**: проверка активности каждые 5 секунд
- **Auto-shutdown**: закрытие через 2 минуты неактивности

---


### Rate Limiting
- Батчинг запросов (950-1500 свечей за раз)
- Пауза между батчами (200ms)
- Лимит одновременных запросов (6)

### Кэширование цен
- ConcurrentHashMap для thread-safety
- Автоматическое обновление каждую секунду
- Хранение в памяти без БД

### Auto-shutdown
Приложение автоматически закрывается при:
- Закрытии последней вкладки браузера
- 2 минутах неактивности
- Запросе `/health?action=close`

---

## 🐛 Известные ограничения

1. **Нет аутентификации** - все запросы публичные
2. **Только spot рынок** - фьючерсы не поддерживаются
3. **Ограничения API бирж**:
    - Bybit: 1000 свечей за запрос
    - Bitget: 1000 свечей за запрос
    - KuCoin: 1500 свечей за запрос
    - OKX: 300 свечей за запрос
4. **Нет персистентности** - все данные в памяти
5. **Single instance** - нельзя запустить несколько копий одновременно (порт 8081)

---

## 📞 Контакты

**Автор:** Максим Буробин

- **Telegram:** [@bur_bone](https://t.me/bur_bone)
- **Email:** m.d.burobin@gmail.com

---

## 📝 Лицензия

Проект создан в учебных целях. Использование на свой страх и риск.

⚠️ **Disclaimer:** Это не финансовый совет. Арбитражная торговля несет риски. Всегда проверяйте данные перед принятием торговых решений.

---

## 🔮 Возможные улучшения

- [ ] Добавить больше бирж (Binance, Gate.io, BitMart)
- [ ] Реализовать WebSocket подписки для real-time обновлений
- [ ] Добавить базу данных для истории арбитража
- [ ] Учет комиссий при расчете прибыли
- [ ] Авторизация и личный кабинет
- [ ] Уведомления о выгодных связках (Telegram бот)
- [ ] Графики и визуализация данных
- [ ] API для интеграции с торговыми ботами
- [ ] Docker контейнеризация
- [ ] Unit и интеграционные тесты

---

**Спасибо за использование! 🚀**